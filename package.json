{
  "name": "rxjs-reactive-web",
  "version": "0.1.0",
  "description": "Pure HTML + RxJS + TypeScript reactive web framework with MVU, components, and routing.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.21"
  }
}
